# navigate to root directory:

cd ukuu

# configure meson:

meson build --prefix=/usr

# navigate to build directory and build project:

cd build && ninja

# (optional) install project locally:

sudo ninja install

